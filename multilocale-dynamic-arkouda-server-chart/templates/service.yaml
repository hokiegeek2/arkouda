apiVersion: v1
kind: Service
metadata:
  name: {{ include "arkouda-server-chart.fullname" . }}
  labels:
    {{- include "arkouda-server-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arkouda-server-chart.selectorLabels" . | nindent 4 }}
