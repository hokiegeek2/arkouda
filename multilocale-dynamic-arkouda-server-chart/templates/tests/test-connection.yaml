apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arkouda-server-chart.fullname" . }}-test-connection"
  labels:
    {{- include "arkouda-server-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arkouda-server-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
