apiVersion: v1
kind: Service
metadata:
  name: {{ include "arkouda-metrics-server-chart.fullname" . }}
  labels:
    {{- include "arkouda-metrics-server-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arkouda-metrics-server-chart.selectorLabels" . | nindent 4 }}
