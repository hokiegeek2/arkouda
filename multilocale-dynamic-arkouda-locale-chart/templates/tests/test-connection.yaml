apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arkouda-locale-chart.fullname" . }}-test-connection"
  labels:
    {{- include "arkouda-locale-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arkouda-locale-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
