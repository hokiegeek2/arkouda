apiVersion: v1
kind: Service
metadata:
  name: {{ include "arkouda-locale-chart.fullname" . }}
  labels:
    {{- include "arkouda-locale-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arkouda-locale-chart.selectorLabels" . | nindent 4 }}
