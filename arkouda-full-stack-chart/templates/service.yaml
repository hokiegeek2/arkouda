apiVersion: v1
kind: Service
metadata:
  name: {{ include "arkouda-full-stack-chart.fullname" . }}
  labels:
    {{- include "arkouda-full-stack-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arkouda-full-stack-chart.selectorLabels" . | nindent 4 }}
